//inserting Element in an array

let arr=[10,20,30,60];
let pos=2;
let elem=45;

for(let i=arr.length-1;i>=0;i--)
{
    if(i>=pos)
    {
        arr[i+1]=arr[i];
        if(i==pos)
        {
            arr[i]=elem;
        }
    }
}

console.log(arr);
